# G2-28 — Gen2 Self-Construction Campaign — First Real Construction Slice

**Status:** LIVE ACTION COMPLETE (slice 1) + 5 of 8 stabilization-evidence categories genuinely evidenced (slice 2) — see "The live action, for real" and "Slice 2" below
**Authority:** G2-00 activation limits + Owner explicit authorization (see below)
**Dependency context:** SC-23 closed (PR #86, merged `2627875`), the last of the 25 SS20 self-construction conditions; `report.self_construction_capable` (internal, 25-condition claim) is now genuinely `True`. The FINAL, combined `self_construction_capable` remains `False`, driven solely by an external Sergeant `NEEDS_WORK` verdict for `g2-27`, exhaustively investigated and confirmed not code-fixable from this repository (`docs/gen2/G2-27-SC23-closure-review-record.md`, "External assurance follow-up" sections).

## Owner authorization — disclosed, not hidden

The repository owner explicitly, twice, with full understanding of what it means, authorized proceeding to G2-28 now — genuinely beginning the Gen1-to-Gen2 construction-execution-authority transfer — treating the open Sergeant condition as a known, disclosed, deferred bug to revisit once Sergeant itself is upgraded, not as something to hide or fabricate as resolved. This departs from every prior milestone's sequencing (each proceeded only after its own gate genuinely, fully passed); the departure is itself carried in this slice's own evidence trail (`src/tenfold/gen2/self_construction_campaign.py`'s `G2_28_OWNER_AUTHORIZATION`, folded into `docs/gen2/G2-28-construction-log.md`'s first entry and into the `AuthorityTransferRecord.stabilization_evidence["observer_predicates"]` category), never omitted from any evidence package (including the one submitted to Sergeant for `g2-28` itself).

## Purpose and scope of THIS slice

G2-28's own Purpose, verbatim: "Prove Gen2 can execute the remaining already-approved roadmap against itself." Its own Acceptance, verbatim: "At least one meaningful remaining Gen2 milestone is constructed and proven using only Gen2 live execution authority."

This slice does **not** claim that Acceptance is satisfied. It does not claim `STABILIZATION_PROVEN`/`IRREVERSIBLY_COMMITTED` for the authority transfer (both require substantial real operational evidence across all 8 mandatory categories, accumulated from genuine repeated use over time — 5 of the 8 categories are explicitly, honestly marked "deferred to a later slice" in `build_g2_28_construction_authority_transfer_policy()`, never filled with placeholder prose pretending completeness). It does not remove Gen1's live construction authority. "Gen2 owns construction-execution authority" is understood here, exactly as G2-21's own Result clause was for Identity/Generation, as "the transfer protocol for this slice is now genuinely exercised," not "live dispatch has switched."

Research before implementation (see this repository's own session record) confirmed there was no existing "switch" to flip: every prior authority-migration milestone (G2-21, G2-23, G2-25) was deliberately scoped to prove its mechanism only, against disposable fixtures — Gen1's real `Foreman` remains, today, the sole dispatcher of every real construction action in this project's history, including the one that built G2-01 through G2-27 themselves. This slice builds the first genuinely live Gen2 dispatch/mutation path.

## What this PR delivers

New module `src/tenfold/gen2/self_construction_campaign.py`, reusing every piece of already-PROVEN machinery directly (nothing re-derived):

- `AuthorityTransferRecord`/`AuthorityTransferStage`/`AuthorityTransferStabilizationPolicy` (G2-02, reused via `constitutional.py`) — `open_g2_28_construction_authority_transfer()` drives `PREPARED → STAGED`, real Python transition plus real, independent Rust re-derivation (`authority_transfer_bridge.rust_check_authority_transfer_transition`), mirroring `authority_transfer.py`'s own G2-21 pattern exactly.
- `campaign_compiler.compile_campaign_program` (G2-07, reused) — `compile_g2_28_first_construction_program()` builds one real, minimal, single-task `CompiledCampaign` for one `MUTATION` obligation routed to `sergeant` assurance.
- `dispatch_lease.gen1_lease_acquire` (G2-11, reused) — a real `tenfold.ownership.LeaseRegistry` lease, genuine Gen1 fencing safety continuing to gate the mutation unchanged by the transfer (the module is explicitly "Gen1 authoritative; Gen2 shadow only" — a safety rail, not construction authority).
- `repository_construction_facility` (SC-23, reused) — `build_live_repository_construction_facility()` wraps SC-23's own already-hardened `DisposableRepositoryConstructionRig`/`gen1_wrap_repository_construction_facility` against a real (not disposable) repository target; `build_live_construction_dispatch()` builds a sealed `TaskPacket` bound to the real lease above (a known, confirmed-by-source-comparison gap: `dispatch_lease.sealed_task_dispatch_digest` builds a fixed G2-11-parity-fixture shape that does not digest-match a real repository-construction task, so this builds its own matching `TaskPacket` instead).
- `effect_census`/`runtime_obligation`/`proof_graph` (G2-18/G2-13/G2-12, reused) — real `ExpectedEffect`/`ObservedEffect` built from real `git` queries (`effect_census.probe_facility_for_observed_effects` doesn't apply to a git-backed facility, intentionally bypassed rather than forced), real `RuntimeObligationCandidateLedger` (one honestly `REJECTED` entry — the census comes back clean and terminal, so no genuine runtime obligation is needed, a positive finding not a skipped check) and two real `HazardRecord`s (mid-commit interruption made unreachable by SC-23's own already-qualified `reconciliation_and_ack_semantics` property; the open Sergeant condition explicitly, honestly disposed as `EXPLICITLY_ACCEPTED_BOUNDED` under the Owner authorization), real `admit_evidence`/`compute_proof_verdict` chain.
- `run_g2_28_external_assurance()` — G2-28's own real, separate Sergeant submission (its own `milestone_id`, its own `changed_files`), structurally identical to `self_construction.py`'s `run_g2_27_external_assurance`, never assuming PASS, raising only on genuine `BLOCK` or reconciliation mismatch.
- `run_g2_28_council_review()` (round-3 addition, see below) — a real, genuine invocation of `council_pin.invoke_pinned_council`/`council.reconcile()`, twice, independently reconciled, matching the same "never trust a single invocation" discipline every other external-assurance call site in this campaign already applies. The first real, non-test call site of that machinery anywhere in Gen2.
- Real write-ahead Chronicle journaling (`chronicle_bridge.open_chronicle`/`append_entry`, round-3 addition) around the two real mutations, mirroring `authority_transfer.py`'s own established G2-21 pattern.
- Observation-cover enumeration (`build_unexpected_branch_effects`, round-3 addition) — every branch's own head is captured before the mutation and re-checked after, so any branch OTHER than the intended target that changed unexpectedly enters the Effect Census as genuine, unattributed residue, matching SC-23's own `list_branches` enumeration-completeness precedent.

## Real local verification (this PR, disposable repos only)

`tests/gen2/test_g2_28_self_construction_campaign.py` — 13 tests, all against a disposable, throwaway local git repository, never the live repository:

- Transfer policy well-formedness; `PREPARED → STAGED` transition in Python and real independent Rust; illegal-transition rejection; owner-authorization disclosure content checks.
- Compiled campaign well-formedness (task IDs, mutation-domain obligation IDs, required assurance = `{"sergeant", "tenfold_council"}`).
- Real Council review invocation, reconciled, honestly reflecting an unresolved Sergeant assurance.
- Live repository rig construction against a real disposable repo; live dispatch building a genuinely self-sealed `TaskPacket` bound to a real lease.
- **Full end-to-end orchestration** (`execute_g2_28_first_construction_slice`) against a disposable repo: one real branch, one real commit, real write-ahead Chronicle journaling (intent + completion entries), real Effect Census with full observation-cover enumeration (clean, no residue), real Runtime Obligation ledger + hazard resolution, a real Proof Graph verdict correctly and honestly reaching `NOT_PROVEN` given the open Sergeant condition (see round 3 below), a real, live Sergeant call for `g2-28`, and a real Council call.
- `main` genuinely untouched, and the working tree's checked-out branch genuinely unchanged (the new commit lands on `gen2/g2-28-first-live-construction` only).
- **Real-commit effect-pair correctness**: a landed commit that genuinely lands on ONE branch while a DIFFERENT, checked target branch was never moved past the original base must report `has_evidence=False` and make the Effect Census raise (round-2 review finding).
- **Observation-cover completeness**: a genuine, unrelated concurrent change to a DIFFERENT branch than the intended target must be caught as real, unattributed residue (round-3 review finding).
- Model-blackout self-check, mirroring `test_g2_27_self_construction_module_itself_respects_model_blackout`.

**Round-2 review findings (CodeRabbit and an independent adversarial-review agent, both reproduced, fixed):**

- **P2 (both sources, independently)**: `build_observed_effect_for_construction_commit`'s `has_evidence` check only proved the target branch NAME existed (`branch in list_branches(rig)`), never that it actually pointed at `landed_sha` — a genuine child-of-`expected_head` commit landing on a DIFFERENT ref while the checked branch stayed unmoved still reported `has_evidence=True`, letting a clean Effect Census attribute a mutation to a branch that never received it. Fixed by resolving the branch's own real current head (`rig.transport.resolve_ref`) and requiring it to equal `landed_sha` exactly, with a new permanent regression test reproducing the exact scenario.
- **P3 (independent adversarial review)**: `build_live_construction_dispatch`'s `resource` parameter was declared but never read — dead code left over from mirroring `repository_construction_facility._dispatch`'s own signature. Removed.
- **Minor (CodeRabbit)**: this doc's own Owner-authorization paragraph claimed the disclosure is folded into `AuthorityTransferRecord.stabilization_evidence["observer_predicates"]` — but the record never actually populated that category. Fixed by genuinely populating `observer_predicates`.

**Round-3 review findings (Codex, all four reproduced and fixed) — these went to the actual integrity of the mechanism, not surface bugs, and materially changed this slice's own honest result:**

- **P1: assurance routing under-specified.** The policy routed every obligation to `("sergeant",)` only — copied from an isolated unit-test fixture pattern, never meant to represent a real authority change. The real, frozen Assurance Matrix (`docs/02-assurance-matrix.md`, Generation 1) is explicit: "Change to Tenfold authority... — Tenfold Council + independent authority review; Owner approval where authority policy changes." Fixed by routing to `("sergeant", "tenfold_council")` and genuinely building `run_g2_28_council_review()` — a real invocation of `council_pin.invoke_pinned_council`, the first real (non-test) call site of that machinery anywhere in Gen2. Disclosed openly (in the policy's own code comment): Council's own invocation is treated as satisfying both "Council" and "independent authority review" together, since Council is the established independent-review body Gen2 already uses elsewhere in this project for that role — a genuinely open scope question, not silently assumed, since no other real Gen2 code has needed to resolve it either. The Matrix's own `assurance_matrix_digest` binding remains a placeholder (`"m" * 64`) — disclosed as a pre-existing, project-wide gap (`proof_transfer.py`, the only other real, non-test module building a `ConstitutionalPolicySet`, uses the identical placeholder), not something this slice invents or attempts to close on its own.
- **P1: verdict/eligibility conflation.** `AssuranceBindingClaim` carries no verdict/eligibility field — `compute_proof_verdict`'s own reconciliation only checks that the two independent copies genuinely agree with each other, never whether the verdict was actually a pass. The original code handed it a claim unconditionally, so Sergeant's genuine `NEEDS_WORK` silently counted as "satisfied." Fixed by gating claim construction on `eligible_for_satisfaction` (both independent copies, for both Sergeant and Council) — when not eligible, the assurance type is simply absent from what's handed to `compute_proof_verdict`, so `required_assurance <= satisfied_assurance` correctly fails.
- **P1: no write-ahead journaling before the real mutation.** `create_branch`/`commit` ran with no verified durable Chronicle intent recorded first — a crash mid-mutation on the real repository would leave no authoritative record to reconcile occurrence against. Fixed with a real Chronicle log (`chronicle_bridge.open_chronicle`/`append_entry`, the same real compiled Rust engine `authority_transfer.py`'s own G2-21 pattern uses): a genuine "intent" entry appended before either mutation, a "completed" entry appended only after both genuinely succeeded.
- **P1: Effect Census only checked the one expected branch.** A concurrent or induced mutation to any OTHER branch would never enter the census at all, since both `expected` and `observed` were built solely for the producer-declared target. Fixed with `build_unexpected_branch_effects()`: every branch's head is captured before the mutation and re-enumerated after, so any branch other than the target that changed unexpectedly enters the census as genuine, unattributed residue — matching SC-23's own established `list_branches` enumeration-completeness precedent.

**Corrected result, and why the earlier claim was wrong:** with all four fixes genuinely in place, `execute_g2_28_first_construction_slice`'s real, live Sergeant submission for `g2-28` returns `NEEDS_WORK` (**not** eligible for satisfaction) — with the **identical two findings** G2-27's own stuck verdict carries ("Nested iteration pattern may create scaling risk", "Changed exported symbols are called from other files"), on an entirely different, brand-new module with no shared content (further independent evidence these findings aren't meaningfully tied to reviewed content). Because the eligibility gate now correctly excludes an ineligible Sergeant claim, and Council's own `reconcile()` genuinely, honestly reflects that "sergeant" is unresolved (`accepted_for_rebrief=False`), the Proof Graph's real verdict now correctly comes back **`NOT_PROVEN`**. The PR's own earlier text here claimed the verdict "correctly reached PROVEN" — that claim was itself a direct symptom of the verdict/eligibility-conflation bug above, not a genuine, correct result; it has been corrected. `NOT_PROVEN` is the honest, expected outcome given the standing open Sergeant condition, matching this whole campaign's own "an honest FALSE/NOT_PROVEN is a legitimate outcome" discipline.

Full local re-verification: new test file (13/13), full repository sweep (see commit for exact counts — only the 9 known pre-existing Windows-only subprocess failures expected, unrelated to this change).

## The live action, for real

The mechanism from PR #87 (`68ef296`) was run once, by hand, via the human-invoked
`scripts/g2_28_first_live_construction.py` (untracked, matching this repo's own
`scripts/tenfold_g2_XX_council.py` convention — never a pytest test, never run
unattended), against the actual `tenfold-gen2/tenfold` repository at its real,
current `main` (`68ef296`):

- **Branch:** `gen2/g2-28-first-live-construction`
- **Landed commit:** `6002ec06fdc2c6bd3520271c90022dbba810b51d`
- **`main`:** verified unchanged (`git rev-parse main` still `68ef296` after the run; the working tree's checked-out branch never moved off `main` either — the facility mutates only through git plumbing, never `checkout`).
- **Transfer record:** `STAGED` (unchanged from PR #87 — this slice does not attempt `STABILIZATION_PROVEN`).
- **Proof verdict:** `NOT_PROVEN` — expected and correct, not a regression. The real, live Sergeant submission for `g2-28` returned `NEEDS_WORK` (`eligible_for_satisfaction=False`) with three findings: `Changed file has dependent modules that may be affected.`, `Nested iteration pattern may create scaling risk.`, `Changed exported symbols are called from other files.` (one more than the disposable-repo test run surfaced in PR #87 — Sergeant's findings are evidently sensitive to real repository history/dependency graph, not just file content, which is itself useful signal that this condition is genuinely about the review target and not a fixture artifact). Council's own `reconcile()` genuinely, honestly reflects the unresolved Sergeant assurance (`reconciled=True`, `accepted_for_rebrief=False`). Per the eligibility gate fixed in round 3, neither assurance claim was constructed, so `compute_proof_verdict` correctly returned `NOT_PROVEN`.
- **Chronicle:** two entries written (`g2-28-construction-intent`, `g2-28-construction-completed`) to a scratch log outside the tracked tree (`d:\tmp\g2-28-live\g2-28-construction.chronicle`), verified re-openable with `last_sequence >= 2`.
- **Disclosure carried through:** `docs/gen2/G2-28-construction-log.md` (new, landed by the live commit itself) records the transfer ID, branch, lease ID, and the full `G2_28_OWNER_AUTHORIZATION` block verbatim — the same disclosure that was in every prior evidence package, now also in the real, permanent commit history.

**An unplanned but necessary fix, found only by running against the real repository:** `LocalGitRepositoryTransport.__init__` failed immediately with `fatal: detected dubious ownership` — this machine's D: drive doesn't report file ownership, and `_environment()` deliberately blanks `GIT_CONFIG_GLOBAL`/`GIT_CONFIG_NOSYSTEM` (hardened isolation from ambient config), so the operator's own machine-wide `safe.directory=*` trust never reached the subprocess. No prior test caught this because every existing test (disposable-repo fixtures) builds its throwaway repo under a temp directory that doesn't share this drive's quirk — this is the first execution against the real repository, on this drive, in the project's history. Fixed in `src/tenfold/local_git_transport.py`'s `_environment()` by re-declaring `safe.directory` scoped to exactly the roots this transport instance has explicitly registered, via git's own `GIT_CONFIG_COUNT`/`GIT_CONFIG_KEY_N`/`GIT_CONFIG_VALUE_N` environment mechanism — no file written, no global/system config touched, and narrower than the machine's own blanket `*` trust (only the specific registered repository roots are trusted, matching this module's existing least-privilege posture elsewhere). Two new regression tests added to `tests/test_local_git_transport.py` proving the scoping is exact (one entry per registered repo, no leakage). Full existing suite re-run clean afterward: `tests/test_local_git_transport.py` (7/7), `tests/gen2/test_sc23_repository_construction_facility.py` + `tests/test_tf30_campaign.py` (139/139 combined with the transport suite), `tests/gen2/test_g2_28_self_construction_campaign.py` (13/13) — all before and after re-run to confirm no regression.

**Round-1 review findings (Codex, PR #88) — one investigated and rebutted with evidence, one genuine and explicitly deferred by the Owner:**

- **P1: requalify the Facility after changing its transport.** Codex's concern: `local_git_transport.py`'s `safe.directory` fix changes the transport underlying the SC-23-admitted, Trust-Table-qualified repository-construction Facility, and the Facility's `facility_generation` constant doesn't bump to reflect that. Investigated directly rather than dismissed: `qualify_declared_scenarios()` (the harness backing `ADMITTED_REPOSITORY_CONSTRUCTION_FACILITY_IDENTITY`) genuinely re-runs every real scenario — real git mutations, real crash-injection, real generation fencing — fresh on every call; it is not a cached historical result, and `facility.py`'s own already-adversarially-reviewed comment on `_is_admitted_repository_construction_identity` states the trust boundary for this exact Trust Table row is "construction-time code review... and disciplined callers," not a generation bump per dependency change. That harness suite was re-run after the transport fix and passed genuinely (139/139, see above), so the qualification evidence is fresh, not stale. Separately: the fix doesn't weaken what `dubious ownership` actually protects against (an attacker-controlled repo silently trusted in a shared location) — it scopes trust only to paths this transport has already explicitly registered and independently validated (symlink rejection, device/inode identity checks), never an unvalidated or caller-supplied path. Treated as investigated and closed, not waved off.
- **P1: block live G2-28 work until G2-27 assurance passes.** Codex correctly identifies that `docs/00-founding-authority.md` (point 10) requires a *formal* Assurance Matrix amendment — versioned/digested, independently reviewed, impact-analyzed, Owner-approved, producing a new immutable generation — to legitimately alter mandatory assurance; a conversational Owner authorization, however explicit and repeated, is not procedurally that mechanism. This is a genuine, correctly-identified gap, not a false positive. The Owner's explicit decision (given this exact finding): pack it alongside the already-deferred Sergeant `NEEDS_WORK` condition on `g2-27` and revisit both together once Sergeant itself is upgraded and can clarify what the underlying condition actually is — not fixed now, not hidden, carried forward openly as a second, named, deferred item next to the first.

## Does not enable

- The FINAL `self_construction_capable` claim — unchanged by this PR; still `False`, still driven solely by the open external Sergeant condition on `g2-27`.
- G2-28's own Acceptance ("at least one meaningful remaining Gen2 milestone is constructed and proven using only Gen2 live execution authority") — not claimed satisfied by this slice.
- `STABILIZATION_PROVEN`/`IRREVERSIBLY_COMMITTED` for the Gen1→Gen2 construction-execution-authority transfer — still not attempted; see "Slice 2" below for the 5 previously-deferred categories now genuinely evidenced, and why `STABILIZATION_PROVEN` itself remains out of scope.
- Removal of any live Gen1 construction authority — Gen1 remains construction Foreman, unchanged.
- G2-28's own Acceptance is still not satisfied by this one commit alone — one real, mechanism-level construction action landed, with an honest `NOT_PROVEN` verdict given the standing open Sergeant condition, not a claim that a "meaningful remaining Gen2 milestone" is now complete.
- A formal Assurance Matrix amendment for proceeding past G2-27's unmet gate — not produced by this slice. Codex's PR #88 finding correctly identifies this procedural gap; the Owner has explicitly deferred it (see round-1 findings above) rather than closed it, alongside the Sergeant condition itself.

## Slice 2 — real stabilization evidence (chronicle, recovery, checkpoint, rehearsal)

A second, separate PR added `record_g2_28_transfer_stage_chronicle_events`, `induce_g2_28_transfer_crash_and_recover`, `execute_g2_28_construction_authority_transfer_rehearsal`, and the orchestrating `execute_g2_28_stabilization_evidence_slice` to `self_construction_campaign.py`, closing 5 of the 8 `AuthorityTransferStabilizationPolicy` categories with genuine evidence in place of slice 1's honest placeholders — mirroring `authority_transfer.py`'s own G2-21 precedent mechanism-for-mechanism (that module already solved this identical problem for the Identity/Generation authority slice):

- **`required_chronicle_events`**: a real Chronicle log (`chronicle_cli`, compiled Rust, invoked via real subprocess calls — never in-memory) records `g2-28-construction-transfer-staged`/`-soft-committed`/`-stabilizing` for a disposable transfer record driven `PREPARED -> STAGED -> SOFT_COMMITTED -> STABILIZING`, each entry's own `payload_digest` a genuine digest of that record's real content *and* the real `G2_28_TRANSFER_ID` this evidence is gathered on behalf of (not a canned string, and not authenticating only the disposable stand-in).
- **`required_external_checkpoints`**: the `SOFT_COMMITTED` entry's `(sequence, digest)` is durably persisted (an explicit `os.fsync` barrier, not merely `write_text()`) to a file in a genuinely separate directory (a fresh `tempfile.mkdtemp()` root, never a sibling of the chronicle log), read back, and reconciled against a freshly re-opened chronicle head — both its sequence *and* an independently re-dumped entry digest (`dump_as_chronicle_events`, a second real subprocess call), never the in-memory checkpoint object for either side — via `chronicle_bridge.check_checkpoint`, verified *before* the `STABILIZING` entry is appended so the checkpoint provably predates it. Tamper tests confirm a mismatched sequence, a mismatched digest, and a corrupted persisted checkpoint all genuinely raise `ChronicleCliError`.
- **`required_induced_failure_scenarios`** / **`required_recovery_results`**: a genuinely torn/partial write (the record's own serialized JSON truncated mid-write) is first proven to be rejected by the real recovery subprocess boundary, before the `STABILIZING`-stage record is durably, completely written to disk and reconstructed by a genuinely separate Python subprocess (`sys.executable -c <script>`, never the parent's in-memory object) which independently re-derives its stage; the parent then reloads from the same file to continue — recovery and continuation are paired, not asserted independently, and the boundary is shown to distinguish corrupted persistence from a genuine one, not just round-trip a happy path.
- **`abort_reinstatement_conditions`**: a genuinely separate, disposable rehearsal record (`G2_28_REHEARSAL_TRANSFER_ID`, never merged with the real transfer) is driven `PREPARED -> STAGED -> ABORTED` via real `.transition()` calls (both legal edges per `constitutional.py`'s allowed-transition table). Reinstatement uses GENUINE authority fencing, not merely a policy-schema-version bump (round-1 review finding, corrected — see below): the rehearsal's own real `tenfold.ownership.LeaseRegistry` lease is genuinely fenced (`gen1_lease_fence`, the same machinery `gen1_lease_acquire` already uses for the real construction lease in slice 1), its old `(epoch, generation)` fencing token is proven rejected (`gen1_lease_validate_token` returns `False`), and only then is a fresh lease acquired under a new epoch and proven valid — before also reopening `PREPARED -> STAGED` (via the existing, unmodified `open_g2_28_construction_authority_transfer`) under a fresh `stabilization_policy_generation`, kept as additional, non-load-bearing context rather than the fencing proof itself.

`STABILIZATION_PROVEN`/`IRREVERSIBLY_COMMITTED` remain deliberately out of scope — `irreversible_commit_conditions` (the 8th category) is unchanged, since `AuthorityTransferRecord.transition()` only evidence-gates entry into `STABILIZATION_PROVEN` specifically, and that requires substantial real operational evidence accumulated from genuine repeated use over time, not one slice's worth. All of this slice is disposable-fixture-only (a caller-supplied `work_dir`) — unlike slice 1, no live-repository action or human-invoked script was needed, matching `authority_transfer.py`'s own G2-21 precedent, which likewise has no live-execution step of its own.

**Round 1-3 review findings (Codex, all genuine, all fixed):** the description above already reflects the corrected mechanisms. Summary of what real adversarial review actually caught and closed: (1) the external checkpoint's "local head" digest re-echoed an in-memory object instead of being independently re-derived; (2) a clean write-then-read across a subprocess boundary never exercised a genuine interruption; (3) reinstatement bumped a policy-schema-version field rather than performing real authority fencing; (4) the gathered evidence was orphaned from the real transfer record; (5) `.validate()` would have rejected the bound evidence's category keys outright; (6) the checkpoint file was a sibling of the chronicle log it anchors, sharing its failure domain; (7) chronicle entry digests were canned strings, not real record content; (8) the checkpoint write had no durability barrier; (9) the chronicle digests still didn't bind the *real* transfer's identity, only the disposable demonstration record's. Each is a genuine, reproduced finding, fixed with a real mechanism change and a new permanent regression test — not disclosed-and-skipped.

**Real local verification:** 13 new/updated tests (`tests/gen2/test_g2_28_self_construction_campaign.py`), full file 26/26. Full `tests/gen2/` suite re-run clean after every round (1305-1308 passed, zero regressions).

## Next slice

- After PR #88 merged, re-ran `execute_self_construction_gate()`: confirmed unchanged headline verdict, as expected — this slice doesn't touch G2-27's own Sergeant condition, but the transfer record and construction log are real, checkable state, and this is the first Gen2 milestone in the project's history built by a genuinely live Gen2 dispatch/mutation path rather than Gen1's `Foreman`.
- Future slices: eventually enough accumulated real operational evidence across all 8 categories, from genuine repeated use over time, to honestly attempt `STABILIZATION_PROVEN`.
- **Two named, openly-carried deferred items, to be revisited together once Sergeant is upgraded:** (1) the Sergeant `NEEDS_WORK` verdict on `g2-27`'s own frozen evidence, confirmed not code-fixable from this repository, and further confirmed by a deep investigation into Sergeant's own detector logic (real bugs found and fixed upstream, merged there — but this specific finding turned out to be structurally accurate, not a false positive, so it did not change); (2) the formal Assurance Matrix amendment PR #88's Codex review correctly identified as the procedurally-correct way to have authorized proceeding past that gate, which the Owner deferred rather than performed ad hoc. Both explicitly deferred until Tenfold Gen2 as a whole is complete, per the Owner's own direction.
