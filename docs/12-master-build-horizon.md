# Tenfold Master Build Horizon

**Status:** CANONICAL BUILD-HORIZON / RECOVERY MAP  
**Current authority:** TF-00 + frozen G2-00 for currently authorised work  
**Important:** this document defines where the original Tenfold core vision is considered fundamentally built. It does **not** activate Gen 2.1, Gen 2.2, assign future milestone numbers, or replace the separate authority/review required for those generations.

---

## 1. Why this document exists

`docs/08-gen2-roadmap.md` is the frozen **Gen-2.0 construction roadmap** and therefore correctly ends at:

```text
G2-30 — Preferred Runtime Gate
```

That is **not** the end of the complete Tenfold core-build vision.

The complete build horizon is broader:

```text
GEN 1
founding execution system
        ↓
GEN 2.0
constitutional successor
        ↓
GEN 2.1-CLASS GENERATION
scale / incrementality
        ↓
GEN 2.2-CLASS GENERATION
institutional method learning
        ↓
TENFOLD CORE VISION COMPLETE
```

After that boundary, future generations are normal product evolution rather than evidence that the original Tenfold build was unfinished.

---

## 2. Current completed foundation — Gen 1

Gen 1 already establishes the founding execution system:

```text
approved roadmap
    ↓
deterministic/model-free Foreman
    ↓
safe dependency-frontier execution
    ↓
bounded workers / Facilities
    ↓
evidence / Council / assurance
    ↓
proof
```

Founding roadmap:

```text
TF-01 … TF-31
```

Status:

```text
COMPLETE / QUALIFIED
```

Gen 1 is the live construction system for Gen 2 until the approved crossover boundary.

---

## 3. Current active major build — Gen 2.0

Gen 2.0 solves the constitutional problem:

```text
How can Tenfold become stronger,
self-hosting and authority-safe
without turning optimisation or AI
into project authority?
```

Its frozen roadmap is:

```text
G2-01 … G2-30
```

Key transition:

```text
Gen 1 builds Gen 2
        ↓
shadow / hybrid
        ↓
G2-27 Self-Construction Minimum
        ↓
Gen 2 may execute the already-approved remainder
        ↓
G2-29 clean repository-only qualification
        ↓
G2-30 preferred-runtime gate
```

At G2-30:

> **Gen 2.0 is complete.**

But the original Tenfold core vision is not yet considered fundamentally complete, because scale/incrementality and institutional learning remain separately reserved future classes.

---

## 4. Next major build class — Gen 2.1 scale / incrementality

Exact Gen-2.1 architecture and milestone numbering do **not** exist yet and must not be invented from this document.

Before implementation, Gen 2.1 requires its own:

```text
authority proposal
        ↓
hostile review
        ↓
freeze
        ↓
detailed roadmap
        ↓
construction by the then-current qualified Tenfold
        ↓
repository-only qualification
```

Frozen G2-00 already excludes from Gen-2.0 activation the Gen-2.1-class capabilities:

```text
Work Cells optimisation
Execution Forest optimisation
capability-cover optimisation
evidence reuse
minimal re-prove optimisation
advanced resource optimisation
```

The intended outcome of the separately approved scale/incrementality generation is:

```text
same constitutional correctness
        +
less duplicated labour
less unnecessary re-proof
better safe parallelism
better incremental execution
better resource use
higher throughput
```

### Gen-2.1-class completion condition

Do not call this layer complete merely because optimisation code exists.

The future authority should require proof that Tenfold can execute materially larger/repeated campaigns with the same constitutional guarantees while measurably reducing avoidable work and re-proof.

Only a separately approved Gen-2.1 authority may define the exact proof threshold.

---

## 5. Final core-build class — Gen 2.2 institutional evolution

Exact Gen-2.2 architecture and milestone numbering also do **not** exist yet.

Before implementation it requires separate:

```text
authority
review
freeze
roadmap
qualification
```

Frozen G2-00 excludes from Gen-2.0 activation the institutional-learning class including:

```text
Experience Graph
Method Foundry
Method Packages
Method Registry
learned scheduling
adaptive placement
organisational reflex learning
```

The intended institutional loop is:

```text
CAMPAIGN
   ↓
measured outcomes
   ↓
method observation
   ↓
method candidate
   ↓
controlled challenge / experiment
   ↓
repeat / transfer evidence as required
   ↓
independent qualification
   ↓
PROVEN METHOD
   ↓
future approved campaigns may use it
   ↓
more evidence
   ↺
```

Standing law:

```text
learned method != project authority
```

Institutional learning may improve **how** approved engineering is executed. It may never decide project truth, requirements, mandatory assurance, proof, Root Authority, or architecture.

### Gen-2.2-class completion condition

This layer is conceptually complete only when the separately approved implementation can:

```text
observe campaign execution
        ↓
measure method outcomes
        ↓
form candidate improvements
        ↓
challenge them under controlled evidence
        ↓
reject weak/non-transferable methods
        ↓
independently qualify repeatable improvements
        ↓
promote proven methods without widening project authority
        ↓
use proven methods on later approved campaigns
```

and demonstrate that the resulting institutional knowledge measurably improves future engineering without constitutional regression.

---

## 6. Tenfold core-vision completion boundary

The original Tenfold build vision should be considered fundamentally complete only after all three capabilities are real and qualified:

### A. Execute engineering constitutionally

```text
approved roadmap
→ compile/organise work
→ execute safely
→ recover
→ independently prove completion
```

Gen 1 establishes the founding version; Gen 2.0 constitutionalises and self-hosts it.

### B. Execute efficiently at scale

```text
less duplicated labour
less unnecessary re-proof
better safe parallelism
incremental execution
better resource utilisation
```

This is the intended separately approved Gen-2.1 class.

### C. Improve execution from evidence

```text
experience
→ measurement
→ candidate method
→ challenge
→ qualification
→ proven method
→ later execution improves
```

This is the intended separately approved Gen-2.2 class.

Then the finish line is:

```text
GEN 1                 ✅ founding execution
        ↓
GEN 2.0               constitutional/self-hosting successor
        ↓
GEN 2.1-CLASS         scale / incrementality proven
        ↓
GEN 2.2-CLASS         institutional learning proven
        ↓
════════════════════════════════════════
TENFOLD CORE VISION COMPLETE
════════════════════════════════════════
```

This boundary has **no current G2 milestone number** because the future generations have not yet received their separate frozen authorities and roadmaps.

Do not invent a fake `G2-XX` completion milestone.

---

## 7. What changes after core-vision completion

Before the boundary, the operating mode is primarily:

```text
BUILD TENFOLD
```

After the boundary, the normal mode becomes:

```text
OPERATE TENFOLD
        ↓
OBSERVE
        ↓
QUALIFY BETTER METHODS
        ↓
IMPROVE EXECUTION
        ↓
OCCASIONALLY EVOLVE ARCHITECTURE
```

Future generations do not imply the core project was unfinished.

They are successor product generations built from a complete operating institution.

---

## 8. Recursive successor construction remains required

Core-vision completion also requires the current qualified Tenfold to retain the ability to construct any separately approved successor:

```text
CURRENT QUALIFIED TENFOLD Gn
        ↓
Owner / external authority approves G(n+1)
        ↓
successor authority + roadmap frozen
        ↓
Gn builds G(n+1)
        ↓
shadow / hybrid
        ↓
authority migration
        ↓
self-construction gate
        ↓
repository-only proof
        ↓
G(n+1) may become current qualified Tenfold
        ↺
```

Method learning never creates successor authority by itself.

---

## 9. Roadmap hierarchy

A zero-context chat must distinguish three different artifacts:

```text
docs/08-gen2-roadmap.md
= CURRENT FROZEN IMPLEMENTATION ROADMAP
= Gen 2.0 only
= G2-01 … G2-30


docs/11-generational-evolution-map.md
= GENERATIONAL MECHANISM MAP
= how build → operate → learn → build successor repeats


docs/12-master-build-horizon.md
= MASTER CORE-BUILD HORIZON
= where the original Tenfold vision is considered fundamentally complete
```

Therefore, if asked:

> “Where does the current approved roadmap end?”

Answer:

```text
G2-30 — Preferred Runtime Gate
```

If asked:

> “When is Tenfold itself fundamentally built?”

Answer:

```text
after separately authorised and qualified
Gen-2.1-class scale/incrementality
+
Gen-2.2-class institutional learning
+
recursive successor-construction capability remains proven
```

Do not collapse those two answers into one.

---

## 10. Authority boundary

This document does not authorise:

```text
starting Gen 2.1
starting Gen 2.2
choosing future milestone numbers
promoting candidate concepts into frozen scope
changing G2-00
changing TF-00
```

It records the **master build horizon** only.

The current next authorised implementation milestone remains whatever live repository evidence establishes under `docs/08-gen2-roadmap.md`; at the present freeze baseline that is G2-01.
